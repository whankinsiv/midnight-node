#client
# Read bridge UTXOs from Acropolis gRPC

Switch the node's Acropolis mainchain follower bridge datasource from the mock implementation to the real `GetBridgeUtxos` gRPC flow, and vendor the Acropolis bridge protobuf messages needed for that client.

JIRA: N/A

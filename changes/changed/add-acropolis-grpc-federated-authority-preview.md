#client #local-environment
# Add Acropolis gRPC federated authority support in preview

Add a federated-authority mainchain follower data source backed by the Acropolis
`MidnightState` gRPC service, and wire the preview docker orchestration to run the
Acropolis `midnight-indexer-preview` container by default.

PR: https://github.com/whankinsiv/midnight-node-acropolis/pull/4
JIRA: N/A
